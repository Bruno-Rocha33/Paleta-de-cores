/* Place your JavaScript in this file */


    function multivisi_cor1() {
      /* Get the text field */
      var copyText = document.getElementById("multivisi_cor1");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }

    function Multivisi_cor2() {
      /* Get the text field */
      var copyText = document.getElementById("Multivisi_cor2");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function Multivisi_cor3() {
      /* Get the text field */
      var copyText = document.getElementById("Multivisi_cor3");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function Multivisi_cor4() {
      /* Get the text field */
      var copyText = document.getElementById("Multivisi_cor4");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function Multivisi_cor5() {
      /* Get the text field */
      var copyText = document.getElementById("Multivisi_cor5");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  


  
    function sorvetec_cor1() {
      /* Get the text field */
      var copyText = document.getElementById("sorvetec_cor1");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function sorvetec_cor2() {
      /* Get the text field */
      var copyText = document.getElementById("sorvetec_cor2");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function sorvetec_cor3() {
      /* Get the text field */
      var copyText = document.getElementById("sorvetec_cor3");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function sorvetec_cor4() {
      /* Get the text field */
      var copyText = document.getElementById("sorvetec_cor4");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  


  
    function brutatec_cor1() {
      /* Get the text field */
      var copyText = document.getElementById("brutatec_cor1");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function brutatec_cor2() {
      /* Get the text field */
      var copyText = document.getElementById("brutatec_cor2");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function brutatec_cor3() {
      /* Get the text field */
      var copyText = document.getElementById("brutatec_cor3");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function brutatec_cor4() {
      /* Get the text field */
      var copyText = document.getElementById("brutatec_cor4");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function brutatec_cor5() {
      /* Get the text field */
      var copyText = document.getElementById("brutatec_cor5");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  


  
    function visuteccrafts_cor1() {
      /* Get the text field */
      var copyText = document.getElementById("visuteccrafts_cor1");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function visuteccrafts_cor2() {
      /* Get the text field */
      var copyText = document.getElementById("visuteccrafts_cor2");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function visuteccrafts_cor3() {
      /* Get the text field */
      var copyText = document.getElementById("visuteccrafts_cor3");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function visuteccrafts_cor4() {
      /* Get the text field */
      var copyText = document.getElementById("visuteccrafts_cor4");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function visuteccrafts_cor5() {
      /* Get the text field */
      var copyText = document.getElementById("visuteccrafts_cor5");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }


    function visuteccrafts_cor6() {
      /* Get the text field */
      var copyText = document.getElementById("visuteccrafts_cor6");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  


  
    function visutec_cor1() {
      /* Get the text field */
      var copyText = document.getElementById("visutec_cor1");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function visutec_cor2() {
      /* Get the text field */
      var copyText = document.getElementById("visutec_cor2");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function visutec_cor3() {
      /* Get the text field */
      var copyText = document.getElementById("visutec_cor3");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  

  
    function visutec_cor4() {
      /* Get the text field */
      var copyText = document.getElementById("visutec_cor4");

      /* Select the text field */
      copyText.select();
      copyText.setSelectionRange(0, 99999); /* For mobile devices */

      /* Copy the text inside the text field */
      navigator.clipboard.writeText(copyText.value);

      /* Alert the copied text 
      alert("Copied the text: " + copyText.value);*/
    }
  